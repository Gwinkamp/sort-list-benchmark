[tool.poetry]
name = "sort-list-benchmark"
version = "1.0.0"
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"
pytest = "^8.3.4"
pytest-benchmark = "^5.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
